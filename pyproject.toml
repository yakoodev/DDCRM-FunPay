[build-system]
requires = ["setuptools>=69.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ddcrm-funpay-worker"
version = "0.1.0"
description = "DDCRM FunPay worker runtime (OpenAPI v2 worker contract wrapper)"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.116.0",
  "uvicorn>=0.35.0",
  "pydantic>=2.11.0",
  "cryptography>=45.0.0",
  "requests>=2.31.0",
  "requests_toolbelt>=0.10.1",
  "beautifulsoup4>=4.12.0",
  "lxml>=5.3.0"
]

[project.optional-dependencies]
dev = [
  "pytest>=8.4.0",
  "httpx>=0.28.0"
]

[project.scripts]
ddcrm-funpay-worker = "ddcrm_funpay_worker.main:run"

[tool.setuptools.packages.find]
include = ["ddcrm_funpay_worker*"]

[tool.uvicorn]
factory = false
